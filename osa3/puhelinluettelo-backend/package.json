{
  "name": "puhelinluettelo-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "del -rf dist && cd ../puhelinluettelo-frontend && npm run build && copy -r dist ../puhelinluettelo-backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Minna Karhu",
  "license": "ISC",
  "description": "Backend for Puhelinluettelo",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongodb": "4.1",
    "mongoose": "^8.18.2",
    "morgan": "^1.10.1"
  }
}
